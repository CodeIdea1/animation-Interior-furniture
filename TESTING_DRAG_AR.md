# 🧪 دليل اختبار ميزة السحب

## 🚀 خطوات الاختبار

### 1. تشغيل المشروع
```bash
npm run dev
```

### 2. فتح المتصفح
انتقل إلى: `http://localhost:3000`

### 3. فتح Console
- **Chrome/Edge**: `F12` أو `Ctrl+Shift+J` (Windows) / `Cmd+Option+J` (Mac)
- **Firefox**: `F12` أو `Ctrl+Shift+K` (Windows) / `Cmd+Option+K` (Mac)
- **Safari**: `Cmd+Option+C` (Mac)

### 4. مراقبة الرسائل
بعد تحميل الصفحة، يجب أن ترى في Console:

```
✅ الأنيميشن الأولي انتهى - يمكنك الآن سحب المصباح!
🔧 تم تفعيل ميزة السحب على المصباح
```

### 5. اختبار السحب

#### اختبار الماوس:
1. انتظر 3 ثوانٍ بعد تحميل الصفحة
2. ضع المؤشر فوق المصباح light2 (يجب أن يتحول إلى يد 🖐️)
3. اضغط بالماوس واسحب
4. عند الضغط يجب أن ترى:
   ```
   🎯 بدأ السحب! { clientX: 500, clientY: 300 }
   ```
5. عند الحركة يجب أن ترى:
   ```
   🚀 يتحرك: { deltaX: 50, deltaY: 30 }
   🚀 يتحرك: { deltaX: 60, deltaY: 40 }
   ...
   ```

#### اختبار اللمس (على الموبايل):
1. انتظر 3 ثوانٍ
2. المس المصباح واسحب بإصبعك
3. يجب أن ترى نفس الرسائل في Console

## ✅ علامات النجاح

- ✅ المؤشر يتحول إلى `grab` (يد مفتوحة)
- ✅ عند الضغط يتحول إلى `grabbing` (يد مغلقة)
- ✅ الصورة تتحرك مع المؤشر فوراً
- ✅ رسائل Console تظهر بشكل صحيح
- ✅ عند الإفلات، المصباح يتأرجح ويرجع

## ❌ علامات الفشل

### إذا لم يتحرك المصباح:
1. **تحقق من Console** - هل ترى رسالة "بدأ السحب"؟
   - ❌ **لا**: المشكلة في event listeners
   - ✅ **نعم**: المشكلة في GSAP

2. **تحقق من انتظار 3 ثوانٍ**
   - هل ترى رسالة "الأنيميشن الأولي انتهى"؟
   - إذا لم تنتظر، سترى: "⏰ انتظر انتهاء الأنيميشن الأولي..."

3. **تحقق من GSAP**
   ```bash
   npm list gsap
   ```
   يجب أن يظهر: `gsap@3.x.x`

### إذا كان المؤشر لا يتغير:
- تحقق من CSS - افتح DevTools → Elements
- ابحث عن `.light2Container`
- تحقق من أن `cursor: grab` موجود

### إذا كانت الحركة بطيئة أو متقطعة:
- تحقق من FPS في Performance Monitor
- أغلق تطبيقات أخرى
- جرب متصفح آخر

## 🔍 استكشاف الأخطاء المتقدم

### فحص العنصر:
1. افتح DevTools → Elements
2. ابحث عن `<div ref={light2Ref}>`
3. تحقق من:
   - ✅ `style="cursor: grab;"`
   - ✅ `class` يحتوي على `light2Container`
   - ✅ العنصر ظاهر (ليس `display: none`)

### فحص Event Listeners:
1. في DevTools → Elements
2. اختر عنصر المصباح
3. اذهب إلى Tab "Event Listeners"
4. يجب أن ترى:
   - ✅ `mousedown`
   - ✅ `touchstart`

### فحص GSAP:
افتح Console واكتب:
```javascript
gsap.version
```
يجب أن يظهر رقم الإصدار مثل: `"3.12.5"`

### اختبار يدوي في Console:
```javascript
// احصل على عنصر المصباح
const light2 = document.querySelector('.light2Container');
console.log('العنصر:', light2);

// جرب تحريكه يدوياً
gsap.set(light2, { x: 100, y: 50 });

// أعد تعيينه
gsap.set(light2, { x: 0, y: '-210%' });
```

## 📱 اختبار على الموبايل

### طريقة 1: Remote Debugging
1. على الكمبيوتر: شغل `npm run dev`
2. اعرف IP الخاص بك: `ipconfig` (Windows) أو `ifconfig` (Mac/Linux)
3. على الموبايل: افتح `http://YOUR_IP:3000`

### طريقة 2: Chrome DevTools
1. وصل الموبايل بالكمبيوتر عبر USB
2. على Chrome: اذهب إلى `chrome://inspect`
3. اختر جهازك وافتح DevTools

## 🎯 نتائج متوقعة

### نجح الاختبار ✅
- الصورة تتحرك **فوراً** مع المؤشر
- الحركة **سلسة** 60 FPS
- التأرجح **واقعي** بعد الإفلات
- لا توجد أخطاء في Console

### فشل الاختبار ❌
- الصورة لا تتحرك نهائياً
- الحركة متقطعة أو بطيئة
- أخطاء في Console
- المؤشر لا يتغير

## 📞 إذا لم ينجح الاختبار

أرسل لي:
1. **Screenshot من Console** (كل الرسائل)
2. **Screenshot من Network Tab** (تحقق من تحميل الملفات)
3. **إصدار المتصفح** الذي تستخدمه
4. **نظام التشغيل** (Windows/Mac/Linux)
5. **هل جربت على متصفح آخر؟**

## 🎬 فيديو توضيحي مفترض

الميزة يجب أن تعمل هكذا:
1. افتح الصفحة
2. انتظر 3 ثوانٍ
3. ضع المؤشر على المصباح (يد 🖐️)
4. اضغط واسحب - **الصورة تتبعك فوراً**
5. ارفع يدك - **تأرجح جميل ثم تستقر**

---

**ملاحظة مهمة:** الكود يستخدم **GSAP** و**React** فقط، لا يحتاج أي مكتبة إضافية!
