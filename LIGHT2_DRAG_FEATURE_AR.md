# ✨ ميزة سحب المصباح (Light2 Drag Feature)

## 📝 الوصف
تم إضافة تفاعل سحب احترافي لصورة المصباح الثاني (light2) في قسم الهيرو.

## 🎯 الوظائف

### 1. **التفعيل التلقائي**
- تبدأ ميزة السحب تلقائياً بعد **3 ثوانٍ** من تحميل الصفحة
- ينتظر انتهاء الأنيميشن الأولي قبل التفعيل

### 2. **السحب التفاعلي**
- **الماوس**: اضغط واسحب الصورة للأسفل
- **اللمس**: المس واسحب على الأجهزة المحمولة
- **الحد الأقصى**: يمكن السحب حتى 150 بكسل للأسفل
- **الاتجاه**: السحب للأسفل فقط (محمي من السحب للأعلى)

### 3. **أنيميشن التأرجح (Pendulum Swing)**
عند ترك الماوس أو رفع الإصبع:
- **تأرجح واقعي** يحاكي حركة البندول الفيزيائية
- **5 مراحل** من التأرجح المتناقص:
  1. عودة قوية مع دوران معاكس (0.6 ثانية)
  2. تأرجح متوسط للجهة المعاكسة (0.5 ثانية)
  3. تأرجح خفيف (0.4 ثانية)
  4. تأرجح أخف (0.3 ثانية)
  5. استقرار نهائي (0.3 ثانية)
- **قوة ديناميكية**: كلما سحبت أكثر، كان التأرجح أقوى

### 4. **التأثيرات البصرية**
- **cursor: grab** → يد مفتوحة عند التحويم
- **cursor: grabbing** → يد مغلقة عند السحب
- **brightness(1.05)** → إضاءة خفيفة عند التحويم
- **brightness(1.1)** → إضاءة أقوى أثناء السحب

## 🛠️ التفاصيل التقنية

### المتغيرات المستخدمة
```typescript
const [isDraggingLight2, setIsDraggingLight2] = useState(false);
const dragStartY = useRef(0);           // نقطة بداية السحب
const currentY = useRef(0);              // المسافة الحالية
const animationFrameId = useRef<number | null>(null); // تحسين الأداء
```

### المعادلات الفيزيائية
```typescript
// حساب المسافة المقيدة
const constrainedY = Math.min(Math.max(deltaY, 0), 150);

// حساب قوة التأرجح
const swingStrength = currentY.current / 150; // نسبة من 0 إلى 1

// الدوران بناءً على السحب
rotation: constrainedY * 0.05 // دوران خفيف أثناء السحب
```

### نقطة الدوران
```css
transformOrigin: 'top center' /* الدوران من الأعلى - كأنها معلقة */
```

## 🎨 خصائص CSS المضافة
```css
.light2Container {
  cursor: grab;
  user-select: none;
  transition: cursor 0.2s ease, filter 0.3s ease;
  will-change: transform, filter; /* تحسين الأداء */
}

.light2Container:hover {
  filter: brightness(1.05);
}

.light2Container:active {
  cursor: grabbing;
  filter: brightness(1.1);
}
```

## 📱 دعم الأجهزة المحمولة
- **معالجات اللمس** (Touch Events):
  - `touchstart` → بداية اللمس
  - `touchmove` → حركة الإصبع (مع منع السكرول)
  - `touchend` → رفع الإصبع
- **passive: false** → للسماح بـ `preventDefault()`

## ⚡ تحسينات الأداء
1. **requestAnimationFrame**: حركة سلسة 60 FPS
2. **cancelAnimationFrame**: إلغاء الإطارات غير المستخدمة
3. **will-change**: تحسين عرض GPU
4. **GSAP Timeline**: أنيميشن محسن

## 🎭 تجربة المستخدم
- **طبيعية**: الحركة تحاكي الفيزياء الحقيقية
- **سلسة**: لا توجد قفزات أو تأخير
- **بديهية**: واضحة من شكل المؤشر والتأثيرات
- **ممتعة**: التأرجح يضيف متعة للتفاعل

## 🔧 التخصيص المتاح

### تغيير الحد الأقصى للسحب
```typescript
const constrainedY = Math.min(Math.max(deltaY, 0), 150); // غير 150 للقيمة المطلوبة
```

### تغيير قوة الدوران
```typescript
rotation: constrainedY * 0.05 // غير 0.05 لزيادة/تقليل الدوران
```

### تغيير مدة التأرجح
```typescript
duration: 0.6 // غير المدة لكل مرحلة من مراحل التأرجح
```

### تغيير وقت التفعيل
```typescript
const timer = setTimeout(() => {
  animationCompleted = true;
}, 3000); // غير 3000 (3 ثوانٍ) للقيمة المطلوبة
```

## 🐛 معالجة الأخطاء
- ✅ منع السحب أثناء الأنيميشن الأولي
- ✅ إلغاء أي أنيميشن GSAP حالي قبل السحب
- ✅ تنظيف event listeners عند unmount
- ✅ معالجة حالة عدم وجود العنصر
- ✅ منع السكرول أثناء السحب على الموبايل

## 📊 الملفات المعدلة
1. **HeroSection.tsx** - منطق السحب والأنيميشن
2. **HeroSection.module.css** - الأنماط والتأثيرات البصرية

## 🎉 النتيجة النهائية
تجربة تفاعلية ممتعة وسلسة تضيف حيوية للموقع وتشجع المستخدم على استكشاف المحتوى!
