# 🎯 تحديث: السحب في جميع الاتجاهات

## ✨ التحديث الجديد

تم تحديث ميزة السحب لتدعم **جميع الاتجاهات** بدلاً من الأسفل فقط!

## 🔄 التغييرات الرئيسية

### قبل التحديث:
- ❌ سحب للأسفل فقط
- ❌ محدود في اتجاه واحد

### بعد التحديث:
- ✅ سحب في **جميع الاتجاهات** (↑ ↓ ← → ↗ ↘ ↙ ↖)
- ✅ حركة حرة 360 درجة
- ✅ دوران ديناميكي حسب الاتجاه
- ✅ تأرجح واقعي في الاتجاه المعاكس

## 🎮 كيف يعمل الآن

### 1. **السحب الحر**
```
اسحب المصباح في أي اتجاه:
- يمين →
- يسار ←
- أعلى ↑
- أسفل ↓
- قطري ↗↘↙↖
```

### 2. **الحد الأقصى**
- المسافة القصوى: **200 بكسل** في أي اتجاه
- دائرة تحرك قطرها 400 بكسل

### 3. **الدوران الديناميكي**
- يدور المصباح حسب اتجاه السحب
- حد أقصى للدوران: **15 درجة**
- الدوران يتبع زاوية السحب

### 4. **التأرجح الواقعي**
- يتأرجح في **الاتجاه المعاكس** لاتجاه السحب
- 4 مراحل من التأرجح المتناقص
- يرجع للموضع الأصلي بسلاسة

## 🧮 المعادلات الفيزيائية

### حساب المسافة:
```typescript
const deltaX = clientX - dragStartX;
const deltaY = clientY - dragStartY;
const distance = Math.sqrt(deltaX * deltaX + deltaY * deltaY);
```

### حساب الزاوية:
```typescript
const angle = Math.atan2(deltaY, deltaX) * (180 / Math.PI);
```

### حساب الدوران:
```typescript
const rotationAmount = Math.min(distance / 10, 15);
rotation: rotationAmount * Math.sin(angle * Math.PI / 180)
```

### التقييد في دائرة:
```typescript
const maxDistance = 200;
const constrainedX = Math.min(Math.max(deltaX, -maxDistance), maxDistance);
const constrainedY = Math.min(Math.max(deltaY, -maxDistance), maxDistance);
```

## 📊 مراحل التأرجح

### المرحلة 1: التأرجح القوي (0.5 ثانية)
```typescript
x: oppositeX * swingStrength * 0.6
y: oppositeY * swingStrength * 0.6
rotation: swingStrength * 15
```

### المرحلة 2: التأرجح المتوسط (0.4 ثانية)
```typescript
x: currentX * swingStrength * 0.3
y: currentY * swingStrength * 0.3
rotation: swingStrength * -8
```

### المرحلة 3: التأرجح الخفيف (0.3 ثانية)
```typescript
x: oppositeX * swingStrength * 0.15
y: oppositeY * swingStrength * 0.15
rotation: swingStrength * 4
```

### المرحلة 4: التأرجح الأخف (0.25 ثانية)
```typescript
x: currentX * swingStrength * 0.05
y: currentY * swingStrength * 0.05
rotation: swingStrength * -2
```

### المرحلة 5: الاستقرار النهائي (0.3 ثانية)
```typescript
x: 0
y: '-210%' (الموضع الأصلي)
rotation: 0
```

## 🎨 أمثلة السحب

### مثال 1: السحب لليمين →
```
1. اسحب 100px لليمين
2. الصورة تتحرك يمين + تدور قليلاً
3. ارفع يدك
4. تتأرجح لليسار ← ثم يمين → ثم يسار ← (أقل)
5. تستقر في المنتصف
```

### مثال 2: السحب للأسفل ↓
```
1. اسحب 150px للأسفل
2. الصورة تتحرك أسفل + تدور
3. ارفع يدك
4. تتأرجح للأعلى ↑ ثم أسفل ↓ ثم أعلى ↑ (أقل)
5. تستقر في المنتصف
```

### مثال 3: السحب قطرياً ↗
```
1. اسحب 120px لأعلى يمين
2. الصورة تتحرك قطرياً + تدور حسب الزاوية
3. ارفع يدك
4. تتأرجح ↙ ثم ↗ ثم ↙ (أقل)
5. تستقر في المنتصف
```

## 🔧 المتغيرات الجديدة

```typescript
const dragStartX = useRef(0);     // نقطة البداية X
const dragStartY = useRef(0);     // نقطة البداية Y
const currentX = useRef(0);       // المسافة الحالية X
const currentY = useRef(0);       // المسافة الحالية Y
```

## 📐 الرسم البياني للحركة

```
        ↑ (-200px)
        |
← ──────●────── → 
(-200)  |  (+200)
        |
        ↓ (+200px)
        
● = نقطة البداية (الموضع الأصلي)
الدائرة = منطقة الحركة المسموحة (قطر 400px)
```

## ⚡ تحسينات الأداء

- ✅ **requestAnimationFrame**: حركة سلسة 60 FPS
- ✅ **cancelAnimationFrame**: إلغاء الإطارات غير المستخدمة
- ✅ **Math.sqrt**: حساب المسافة الفعلية
- ✅ **Math.atan2**: حساب الزاوية الدقيقة
- ✅ **GSAP transformOrigin**: نقطة الدوران من الأعلى

## 🎯 المقارنة

| الميزة | قبل | بعد |
|--------|-----|-----|
| الاتجاهات المدعومة | 1 (أسفل فقط) | 8 (كل الاتجاهات) |
| الحد الأقصى | 150px | 200px |
| الدوران | ثابت | ديناميكي حسب الزاوية |
| التأرجح | عمودي فقط | متعدد الاتجاهات |
| الواقعية | متوسطة | عالية جداً |

## 🎪 تجربة المستخدم

### الآن يمكنك:
- ✨ سحب المصباح لأي جهة تريدها
- 🎯 رسم دوائر وأشكال بالسحب
- 🎪 مشاهدة تأرجح واقعي في كل الاتجاهات
- 🎨 التحكم الكامل في حركة المصباح
- 🎮 تجربة تفاعلية ممتعة أكثر

## 🐛 التصحيح

### إذا كانت الحركة غير سلسة:
1. تحقق من أن `requestAnimationFrame` يعمل
2. افتح DevTools → Performance
3. تحقق من FPS (يجب أن يكون 60)

### إذا كان التأرجح غريباً:
1. تحقق من قيم `swingStrength`
2. عدل معاملات التأرجح (0.6, 0.3, 0.15, 0.05)
3. غير مدة كل مرحلة حسب الحاجة

## 🚀 التخصيص

### تغيير الحد الأقصى للمسافة:
```typescript
const maxDistance = 200; // غير إلى 150 أو 250 حسب الحاجة
```

### تغيير حد الدوران:
```typescript
const rotationAmount = Math.min(distance / 10, 15); // غير 15 لزيادة الدوران
```

### تغيير قوة التأرجح:
```typescript
// غير المعاملات: 0.6, 0.3, 0.15, 0.05
x: oppositeX * swingStrength * 0.6  // جرب 0.7 للمزيد
```

---

✅ **الآن جاهز!** جرب سحب المصباح في جميع الاتجاهات! 🎉
