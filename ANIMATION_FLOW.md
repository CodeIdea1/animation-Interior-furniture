# تدفق الأنيميشن - شرح مفصل

## الفكرة الأساسية

الأنيميشن يحاكي تأثير "الانفجار الخارجي" حيث:
1. الصور تظهر من المركز صغيرة جداً
2. تكبر تدريجياً أثناء الحركة
3. تتحرك نحو حواف الشاشة
4. تستمر في التكبير حتى تخرج بالكامل خارج الشاشة
5. تعود فوراً للمركز وتبدأ من جديد

## اتجاهات الخروج (8 اتجاهات)

```
           top-left    top    top-right
                 ↖      ↑      ↗
                   \    |    /
                     \  |  /
        left  ←  ------●------  →  right
                     /  |  \
                   /    |    \
                 ↙      ↓      ↘
      bottom-left   bottom   bottom-right
```

### توزيع الصور على الاتجاهات:

| الصورة | الاتجاه | الوصف |
|-------|---------|-------|
| 1 | right | خروج من اليمين |
| 2 | left | خروج من اليسار |
| 3 | top | خروج من الأعلى |
| 4 | bottom | خروج من الأسفل |
| 5 | top-right | خروج من الزاوية اليمنى العلوية |
| 6 | top-left | خروج من الزاوية اليسرى العلوية |
| 7 | bottom-right | خروج من الزاوية اليمنى السفلية |
| 8 | bottom-left | خروج من الزاوية اليسرى السفلية |
| 9 | right | (تكرار الدورة) |
| 10 | left | (تكرار الدورة) |
| 11 | top | (تكرار الدورة) |

## Timeline للأنيميشن

```
الوقت (ثواني)   |  الحدث
----------------|--------------------------------------------------
0.00            |  الصورة في المركز (scale: 0, opacity: 0)
0.50            |  ظهور أولي (scale: 0.15, opacity: 0.4)
1.40            |  تكبير متوسط (scale: 0.4, opacity: 0.7) + بدء الحركة
2.50            |  تكبير أكبر (scale: 0.7, opacity: 0.95) + حركة أسرع
3.80            |  تكبير كامل (scale: 1.2, opacity: 1) + حركة سريعة
4.80            |  خروج كامل (scale: 1.6, opacity: 0.9) خارج الشاشة
4.80            |  إعادة تعيين فورية للمركز
4.80+           |  بدء الدورة من جديد
```

## حساب مسافة الخروج

### للاتجاهات الأفقية (يمين/يسار):
```typescript
exitX = ±window.innerWidth * 1.2  // 120% من عرض الشاشة
exitY = random between -300 to +300px  // حركة عمودية عشوائية
```

### للاتجاهات العمودية (أعلى/أسفل):
```typescript
exitY = ±window.innerHeight * 1.2  // 120% من ارتفاع الشاشة
exitX = random between -400 to +400px  // حركة أفقية عشوائية
```

### للزوايا:
```typescript
exitX = ±window.innerWidth * 1.1  // 110% من عرض الشاشة
exitY = ±window.innerHeight * 0.8  // 80% من ارتفاع الشاشة
```

## مراحل الحركة بالتفصيل

### 1. الموضع الابتدائي
```typescript
x: startX (random ±50px من المركز)
y: startY (random ±50px من المركز)
scale: 0
opacity: 0
```

### 2. الظهور (0.5s)
```typescript
scale: 0 → 0.15 (15% من الحجم الأصلي)
opacity: 0 → 0.4 (40% شفافية)
ease: power2.out (تسارع في البداية ثم تباطؤ)
```

### 3. التكبير الأول (0.9s)
```typescript
scale: 0.15 → 0.4
opacity: 0.4 → 0.7
x: startX + (exitX * 0.15)  // تحرك 15% من المسافة
y: startY + (exitY * 0.15)
rotation: ±8 degrees (20% من الدوران النهائي)
ease: power1.out
```

### 4. التكبير الثاني (1.1s)
```typescript
scale: 0.4 → 0.7
opacity: 0.7 → 0.95
x: startX + (exitX * 0.35)  // تحرك 35% من المسافة
y: startY + (exitY * 0.35)
rotation: ±20 degrees (50% من الدوران النهائي)
ease: power1.inOut
```

### 5. التكبير الكامل (1.3s)
```typescript
scale: 0.7 → 1.2
opacity: 0.95 → 1
x: startX + (exitX * 0.7)  // تحرك 70% من المسافة
y: startY + (exitY * 0.7)
rotation: ±32 degrees (80% من الدوران النهائي)
ease: power2.inOut
```

### 6. الخروج النهائي (1.0s)
```typescript
scale: 1.2 → 1.6 (أكبر من الحجم الأصلي)
opacity: 1 → 0.9
x: exitX (الخروج الكامل)
y: exitY (الخروج الكامل)
rotation: ±40 degrees (الدوران الكامل)
ease: power2.in (تسارع قوي للخروج)
```

### 7. إعادة التعيين (فوري)
```typescript
duration: 0 (بدون انتقال)
x: startX
y: startY
scale: 0
opacity: 0
rotation: 0
```

## التأخير بين الصور

كل صورة لها تأخير مختلف لخلق تأثير متتابع:

```typescript
الصورة 1:  delay = 0 * 0.25 = 0.00s
الصورة 2:  delay = 1 * 0.25 = 0.25s
الصورة 3:  delay = 2 * 0.25 = 0.50s
الصورة 4:  delay = 3 * 0.25 = 0.75s
...
الصورة 11: delay = 10 * 0.25 = 2.50s
```

هذا يخلق تأثير "موجة" حيث الصور تظهر واحدة تلو الأخرى.

## Easing Functions المستخدمة

### power2.out
- **الاستخدام**: الظهور الأولي
- **التأثير**: بداية سريعة ثم تباطؤ تدريجي
- **الهدف**: ظهور ناعم وطبيعي

### power1.out
- **الاستخدام**: التكبير الأول
- **التأثير**: تسارع خفيف ثم تباطؤ
- **الهدف**: بدء الحركة بسلاسة

### power1.inOut
- **الاستخدام**: التكبير الثاني
- **التأثير**: تسارع في البداية والنهاية، سرعة ثابتة في الوسط
- **الهدف**: حركة متوازنة

### power2.inOut
- **الاستخدام**: التكبير الكامل
- **التأثير**: تسارع قوي في البداية والنهاية
- **الهدف**: حركة ديناميكية نحو الخروج

### power2.in
- **الاستخدام**: الخروج النهائي
- **التأثير**: تسارع مستمر حتى النهاية
- **الهدف**: خروج سريع وقوي من الشاشة

## نصائح للتخصيص

### لزيادة السرعة العامة
قلل قيم `duration` في جميع المراحل:
```typescript
duration: 0.5 → 0.3  // أسرع بـ 40%
```

### لزيادة مسافة الخروج
زد المضاعفات:
```typescript
window.innerWidth * 1.2 → window.innerWidth * 1.5
```

### لإضافة حركة عشوائية أكثر
زد نطاق القيم العشوائية:
```typescript
(Math.random() - 0.5) * 300 → (Math.random() - 0.5) * 600
```

### لتغيير عدد الاتجاهات
عدّل مصفوفة الاتجاهات:
```typescript
// للتركيز على الاتجاهات الأساسية فقط
const exitDirections = ['right', 'left', 'top', 'bottom'];

// للتركيز على الزوايا
const exitDirections = ['top-right', 'top-left', 'bottom-right', 'bottom-left'];
```

---

تم التطوير بعناية لخلق تجربة بصرية سلسة وجذابة!
