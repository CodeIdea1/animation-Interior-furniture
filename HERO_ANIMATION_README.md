# سيكشن الهيرو مع أنيميشن احترافي

## الوصف
سيكشن هيرو احترافي يحتوي على أنيميشن متقدم للصور باستخدام مكتبة GSAP. الصور تبدأ صغيرة وتقترب تدريجياً مع التكبير، ثم تتباعد أفقياً وتختفي في حلقة مستمرة.

## المميزات

### 1. الأنيميشن الاحترافي
- **الظهور من المركز**: الصور تبدأ من حجم صغير جداً (scale: 0) في مركز الشاشة
- **التكبير التدريجي**: الصور تكبر تدريجياً أثناء الحركة
- **الخروج من جميع الجهات**: الصور تخرج من الشاشة من 8 اتجاهات مختلفة:
  - اليمين (Right)
  - اليسار (Left)
  - الأعلى (Top)
  - الأسفل (Bottom)
  - أعلى يمين (Top-Right)
  - أعلى يسار (Top-Left)
  - أسفل يمين (Bottom-Right)
  - أسفل يسار (Bottom-Left)
- **حركة سلسة**: استخدام GSAP لأنيميشن احترافي مع easing functions متقدمة
- **حلقة مستمرة**: الأنيميشن يتكرر بشكل لا نهائي بدون توقف

### 2. الصور المستخدمة
- **صور الديكور**: من `decor-1.png` إلى `decor5.png` (5 صور)
- **صور الأثاث**: من `furniture1.png` إلى `furniture6.png` (6 صور)
- **المجموع**: 11 صورة تتحرك بشكل متزامن

### 3. النص الثابت
- عنوان في المنتصف: "Spaces for people, made for life."
- تصميم responsive يتكيف مع جميع الشاشات
- تأثيرات ظل وتوهج خفيف

### 4. CSS Module
- استخدام CSS Modules بدلاً من Tailwind
- تصميم مخصص بالكامل
- تأثيرات متقدمة (vignette, radial gradients, drop shadows)

## البنية

```
app/
├── components/
│   ├── HeroSection.tsx          # المكون الرئيسي
│   └── HeroSection.module.css   # التنسيقات
├── page.tsx                      # الصفحة الرئيسية
└── globals.css                   # التنسيقات العامة
```

## مراحل الأنيميشن

### المرحلة 1: الظهور الأولي من المركز (0.5 ثانية)
```typescript
scale: 0 → 0.15
opacity: 0 → 0.4
position: مركز الشاشة
```

### المرحلة 2: التكبير التدريجي مع بدء الحركة (0.9 ثانية)
```typescript
scale: 0.15 → 0.4
opacity: 0.4 → 0.7
x: تحرك 15% من المسافة نحو الهدف
y: تحرك 15% من المسافة نحو الهدف
rotation: دوران خفيف
```

### المرحلة 3: الاستمرار في التكبير والاقتراب (1.1 ثانية)
```typescript
scale: 0.4 → 0.7
opacity: 0.7 → 0.95
x: تحرك 35% من المسافة
y: تحرك 35% من المسافة
rotation: زيادة الدوران
```

### المرحلة 4: التكبير الكامل والحركة السريعة (1.3 ثانية)
```typescript
scale: 0.7 → 1.2
opacity: 0.95 → 1
x: تحرك 70% من المسافة
y: تحرك 70% من المسافة
rotation: دوران أكبر
```

### المرحلة 5: الخروج الكامل من الشاشة (1 ثانية)
```typescript
scale: 1.2 → 1.6 (تكبير أقصى)
opacity: 1 → 0.9
x: الخروج الكامل خارج حدود الشاشة
y: الخروج الكامل خارج حدود الشاشة
```

### المرحلة 6: إعادة التعيين
```typescript
العودة الفورية للمركز بدون انتقال
scale: 0
opacity: 0
ثم بدء الدورة من جديد
```

## التخصيصات المتاحة

### تعديل سرعة الأنيميشن
في ملف `HeroSection.tsx`، غيّر قيم `duration`:

```typescript
duration: 0.8,  // سرعة الظهور
duration: 1.2,  // سرعة الاقتراب
duration: 1.8,  // سرعة الحركة الرئيسية
```

### تعديل اتجاهات الخروج
غيّر الاتجاهات المتاحة في المصفوفة:

```typescript
const exitDirections = ['right', 'left', 'top', 'bottom', 'top-right', 'top-left', 'bottom-right', 'bottom-left'];
// يمكنك إضافة أو إزالة اتجاهات
```

### تعديل مسافة الخروج
زد أو قلل المضاعف لتغيير مسافة الخروج:

```typescript
exitX = window.innerWidth * 1.2; // زد القيمة للخروج أبعد
exitY = window.innerHeight * 1.2;
```

### تعديل عدد الصور
أضف أو احذف صور من المصفوفات:

```typescript
const decorImages = [
  '/decor-1.png',
  '/decor2.png',
  // أضف المزيد هنا
];
```

### تعديل النص
في ملف `HeroSection.tsx`:

```typescript
<h1 className={styles.heroTitle}>
  النص الخاص بك هنا
</h1>
```

## تحسينات الأداء

1. **Will-change property**: لتحسين أداء الأنيميشن
2. **Transform-style: preserve-3d**: لأنيميشن 3D محسّن
3. **Pointer-events: none**: لعدم التداخل مع التفاعلات
4. **Reduced motion support**: دعم المستخدمين الذين يفضلون تقليل الحركة

## التشغيل

```bash
# تثبيت المكتبات
npm install

# التشغيل في وضع التطوير
npm run dev

# البناء للإنتاج
npm run build
```

## المكتبات المستخدمة

- **Next.js 16.3.0**: Framework
- **React 19**: UI Library
- **GSAP**: مكتبة الأنيميشن
- **TypeScript**: Type Safety
- **CSS Modules**: التنسيقات المعزولة

## Responsive Design

- **Desktop**: أحجام صور كاملة (200px)
- **Tablet**: أحجام متوسطة (140px)
- **Mobile**: أحجام صغيرة (100px)
- **النص**: حجم ديناميكي باستخدام clamp()

## ملاحظات مهمة

1. جميع الصور يجب أن تكون في مجلد `/public`
2. الأنيميشن يبدأ تلقائياً عند تحميل الصفحة
3. الأنيميشن متزامن لجميع الصور مع تأخيرات مختلفة
4. يمكن تعديل الألوان والخلفية في ملف CSS Module
5. **الصور تخرج بالكامل خارج الشاشة**: عندما تكبر الصور وتقترب، تستمر في الحركة حتى تخرج تماماً من حدود الشاشة
6. **8 اتجاهات للخروج**: كل صورة تأخذ اتجاه مختلف (يمين، يسار، أعلى، أسفل، والزوايا)
7. **لوب مستمر**: بعد خروج الصورة، تعود فوراً للمركز وتبدأ من جديد بدون توقف

## Browser Support

- Chrome/Edge: ✅ كامل
- Firefox: ✅ كامل
- Safari: ✅ كامل
- Mobile Browsers: ✅ كامل

---

تم التطوير باستخدام GSAP للأنيميشن الاحترافي مع CSS Modules للتنسيقات النظيفة والمعزولة.
